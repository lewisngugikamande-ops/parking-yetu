[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# These tell Netlify to pass ALL VITE_* variables to the build
[context.production]
  environment = { }
