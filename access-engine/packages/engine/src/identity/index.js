const Identity = require('./Identity');
const IdentityId = require('./IdentityId');
const IdentityProfile = require('./IdentityProfile');
const IdentityEvents = require('./IdentityEvents');

module.exports = {
  Identity,
  IdentityId,
  IdentityProfile,
  IdentityEvents,
};
